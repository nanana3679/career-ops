# Nested git repo (user's job-prep archive — keep as separate repo)
job-prep/

# Generated / ephemeral
.resolved-prompt-*
node_modules/
bun.lock
package-lock.json
.update-dismissed
.update-lock

# OS
.DS_Store
*.mov
*.mp4

# Claude Code local
.claude/settings.local.json
.claude/memory/
career-dashboard

# Keep .gitkeep files
!**/.gitkeep
